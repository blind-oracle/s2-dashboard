/* Generated by tools/mkfont.py - DO NOT EDIT.  Glyph artwork is original to this project. */
#ifndef S2_GEN_S2_FONT5X7_H
#define S2_GEN_S2_FONT5X7_H

#include <stdint.h>

#define S2_FONT5X7_FIRST 0x20u       /* space */
#define S2_FONT5X7_LAST 0x7Eu        /* tilde */
#define S2_FONT5X7_COUNT 95u
#define S2_FONT5X7_WIDTH 5u         /* stored column bytes per glyph */
#define S2_FONT5X7_HEIGHT 7u        /* rows per column byte, bit 0 = top */
#define S2_FONT5X7_ADVANCE 6u       /* pen step: WIDTH + 1 blank gap column */
#define S2_FONT5X7_BASELINE 5u      /* row the unaccented glyphs sit on */

/* Monospace bitmap font, column-major.  glyph[x] holds column x (left to
 * right); bit y of that byte is row y (bit 0 top, bit 6 the descender row),
 * bit 7 always 0.  So the pixel at (x, y) is (glyph[x] >> y) & 1u. */
typedef struct {
    const uint8_t *data;   /* COUNT * WIDTH column bytes, ASCII order from 'first' */
    uint8_t first;         /* first code point present */
    uint8_t last;          /* last code point present */
    uint8_t width;         /* column bytes per glyph */
    uint8_t height;        /* rows used inside each column byte */
    uint8_t advance;       /* pen advance per character, in columns */
} s2_font_t;

extern const s2_font_t s2_font5x7;

/* Column bytes of 'c', or of '?' when 'c' is outside the covered range. */
static inline const uint8_t *s2_font5x7_glyph(char c)
{
    unsigned code = (unsigned char)c;
    if (code < S2_FONT5X7_FIRST || code > S2_FONT5X7_LAST) {
        code = (unsigned)'?';
    }
    return &s2_font5x7.data[(code - S2_FONT5X7_FIRST) * S2_FONT5X7_WIDTH];
}

#endif /* S2_GEN_S2_FONT5X7_H */

